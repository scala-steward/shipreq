digraph G {

// BLOCKED
removeS [label="Remove from server:\nUnsafeTypes, ProjectDSL"]
realspa [label="Replace /wip with\nreal SPA using real project"]
testDbCodec [label="Ensure db event codec stability"]
localProd [label="Get a local prod env working"]

// NEXT (client)
node [style=filled fillcolor="#ffcc99"]
newReq [label="Create reqs\n(UI, Protocol, MakeEvent)"]
newRCG [label="Create ReqCodeGroups\n(UI, Protocol, MakeEvent)"]
cfgcss [label="Replace CSS WIP with ScalaCSS"]

// NEXT (server)
node [style=filled fillcolor="#ffcccc"]
realCU [label="MakeEvent with\nContentUpdate cmds"]
devx [label="Get /x working again and document"]
db [label="DB table, event load, event add"]

// COMPLETE
node [style=filled fillcolor="#ddffcc"]
template [label="Create template event"]
tesths [label="Ensure hash scheme stability"]
devmd [label="Update DEV.md"]
nowip [label="Use real RemoteFn responses in WIP"]
hash [label="Hashing"]
tp [label="Trim .protocol code"]
nwo [label="Refactor new world:\n.event\n.protocol\nClientData, Changes, ChangeListener\nUpdateProject"]
remove [label="Remove Δ, δ, Rev, Partition"]
allve [label="All RemoteFns use VerifiedEvents instead of Δ"]
bc [label="Change broadcast from δ\nEither each listener scans events or new δ"]
wip2 [label="WIP to use UpdateProject & VerifiedEvents"]
svetcl [label="Send VerifiedEvents to Client"]
aveic [label="Apply VerifiedEvents in Client"]
bin [label="Use binary for transport"]
asfttr [label="Add separate failure type to RemoteFn"]
rpadt [label="Restore pickleADT\nto Protocol codecs"]
event [label="Events"]
eventap [label="Event application & tests"]
eventbin [label="Binary codecs for events"]

event -> eventap,eventbin
bin -> eventbin
eventbin -> svetcl
eventap -> aveic

asfttr -> svetcl
aveic -> wip2

nowip  -> template
svetcl -> wip2 -> nowip
remove -> tp,rpadt
svetcl -> bc
allve -> remove,nowip
wip2,bc -> allve
realspa -> removeS
remove -> nwo -> devmd

template -> db -> testDbCodec
devx,cfgcss,db -> realspa
hash -> tesths,eventap
devx -> localProd
nowip -> realCU,newReq,newRCG
}

