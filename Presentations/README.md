# Setup

Run `yarn`.

# Usage

1. Create a new `.md`, open one an existing one to edit.
2. Run `./bs serve -s`.
3. Open incognito browser to http://localhost:4100 then select presentation.
4. Edit presentation, save will update the browser.
